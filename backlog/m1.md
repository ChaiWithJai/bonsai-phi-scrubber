# M1 — The reproducibility backbone (ship this first)
*Goal: `airplane-core` + the CLI shell + the eval harness. The reproduction front door: a stranger runs `./run.sh eval` and matches `eval/golden-run.txt`.*
*Depends on: M0 ✅. HITL: Jai reviews before M2.*
*This must land before ANY iOS work (M3). Correctness reproduces on a laptop first.*

> **STATUS (2026-06-25): core path DONE.** `airplane-core` (Rust) builds, clippy-clean, 8/8 unit tests; CLI shell + llama-server `InferenceProvider` adapter; eval harness reproduces **100% recall / 0 leakage / recall-gate GREEN** on the 20-note golden set through owned code (`eval/golden-run.txt`).
> **Key finding:** single-pass recall on Bonsai-1.7B is ~80% and *stochastic*; the pipeline **unions 3 seeded passes** (deterministic) to clear the 99% gate — latency traded for the recall a PHI boundary requires. Over-redactions 124 → precision is the documented next tuning knob.
> **Still open (reproducibility hardening, not core):** T10 model fetch-by-hash in `run.sh` (model currently served via `bonsai/scripts/serve.sh`); Dockerfile (T13b); CLI Sink/SecureStore/Capture impls land with M2. Seeded-multipass determinism expected per llama.cpp seed semantics — re-confirm by running eval twice.

## Core (`crates/airplane-core/`)

- [ ] **M1-T01** — Scaffold the Rust workspace: `crates/airplane-core` (lib) + `shells/cli` (bin). · *done-when:* `cargo build` succeeds; empty lib + bin compile.
- [ ] **M1-T02** — Define the four **port traits** in `airplane-core`: `InferenceProvider` (`complete(messages, json_schema, sampling) -> Result<String>`), `SecureStore`, `Capture`, `Sink`. Document each. · *done-when:* traits compile; doc comments name the contract + the trust-boundary rule for each.
- [ ] **M1-T03** — **Rules executor**: load a pack's recognizers (regex + context window + checksum + deny/allow), return spans. · *done-when:* unit tests over the M0 recognizers find the structured identifiers in sample text.
- [ ] **M1-T04** — **Output hygiene** helpers: `<think>`-strip, JSON-extract (fence-tolerant), schema-validate, clamp. · *done-when:* tested against messy model-style strings.
- [ ] **M1-T05** — **Verifier gate**: re-scan candidate output against the full rule set; **default-deny** on any residual hit; return `pass | block(reason)`. · *done-when:* a note with a residual identifier is BLOCKED; a clean note PASSES; tested both ways.
- [ ] **M1-T06** — **Pipeline orchestration**: `capture → rules ∪ inference → gate → (clean) structured`. Inference via the `InferenceProvider` port (union with rules). · *done-when:* pipeline runs end-to-end with a stub inference provider.
- [ ] **M1-T07** — **Pack loader**: validate pack contract (PHI-blind, no code, signature ref present); load recognizers/schema/policy/sink. · *done-when:* a malformed/code-bearing pack is rejected (pack-blindness).

## CLI shell (`shells/cli/`)

- [ ] **M1-T08** — `InferenceProvider` adapter: HTTP client to `llama-server` `/v1/chat/completions` with `response_format: json_schema` and explicit sampling (`temp 0.5, top_k 20, top_p 0.85`). Model selected by flag (F16/Metal or TQ2_0/CPU). · *done-when:* hits a local llama-server and returns constrained JSON. (See `docs/seed/bonsai-ecosystem-brief.md`; mirror `gemini-music` `bonsai_client.py`.)
- [ ] **M1-T09** — Stub `SecureStore` (encrypted file), `Capture` (stdin/arg), `Sink` (mock/stdout) adapters for the CLI. · *done-when:* `./run.sh scrub "<text>"` prints a scrubbed record.
- [x] **M1-T10** ✅ — **Model fetch-by-hash** in `scripts/serve-model.sh`: download the pinned GGUF by repo commit, verify SHA-256 before serving, and fail loud on mismatch. · *done-when:* tampered hash aborts.

## Eval harness + entrypoint

- [ ] **M1-T11** — Eval harness: run the scrubber over the M0 golden notes, score per-entity **recall/precision + leakage rate**, run the gate, print a result. Greedy/temp-0, pinned hash/commit. · *done-when:* produces a deterministic report.
- [ ] **M1-T12** — Commit `eval/golden-run.txt` (the expected output) and wire `./run.sh eval` to match it. · *done-when:* `./run.sh eval` matches the committed file on a clean checkout.
- [ ] **M1-T13** — `./run.sh` with verbs `eval | demo | scrub | gates`; `Dockerfile` for a hermetic Tier-1 path. · *done-when:* `docker build` + `./run.sh eval` in-container reproduces.
- [ ] **M1-T14** — Implement `./run.sh gates`: `recall`, `leakage`, `pack-blindness` (the rest stubbed as `skip` until their milestone). · *done-when:* a leaky pack BLOCKS; a clean pack PASSES.

## Done-when (milestone)
A stranger clones, runs `./run.sh eval`, and matches `golden-run.txt` — proving the scrub is real and reproducible **with no phone**. → **HITL: Jai review before M2.**
